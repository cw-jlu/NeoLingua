# 性能测试和商业指标生成依赖
# 使用较旧但稳定的版本，避免编译问题

# HTTP客户端
aiohttp>=3.8.0
requests>=2.28.0

# 数据处理（使用较旧版本避免编译）
pandas>=1.5.0,<2.0.0
numpy>=1.23.0,<1.25.0

# 图表生成
matplotlib>=3.6.0
seaborn>=0.12.0

# Excel处理
openpyxl>=3.0.0
xlsxwriter>=3.0.0

# 系统监控
psutil>=5.9.0

# 进度条
tqdm>=4.64.0

# 日期时间
python-dateutil>=2.8.0
